repos:
  - repo: https://github.com/cachix/pre-commit-hooks.nix
    rev: v1.3.0
    hooks:
      - id: statix
        args: [check]
      - id: deadnix
        args: [--fail]
      - id: nixpkgs-fmt
      - id: shellcheck
